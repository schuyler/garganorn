[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "garganorn"
version = "0.1.0"
description = "An ATProtocol XRPC server for org.atgeo"
readme = "README.md"
requires-python = ">=3.9"
authors = [
    {name = "Schuyler Erle"}
]
dependencies = [
    "duckdb==1.4.4",
    "gunicorn",
    "lexrpc[flask]",
    "pyyaml",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "black",
    "isort",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
filterwarnings = ["ignore::DeprecationWarning"]

[tool.setuptools]
packages = ["garganorn"]
package-data = {"garganorn" = ["lexicon/*.json", "sql/**/*.sql"]}
